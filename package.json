{
  "name": "rewardmaths",
  "version": "5.0.0",
  "private": true,
  "type": "module",
  "description": "Static site — package.json exists only so Node can run the engine tests as ES modules",
  "scripts": {
    "test": "node tests/run.js",
    "simulate": "node tests/simulate.js"
  }
}
