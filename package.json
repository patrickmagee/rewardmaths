{
  "name": "reward-maths",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "description": "Reward Maths - category-based math practice game (zero runtime dependencies; dev deps are tests only)",
  "scripts": {
    "test": "vitest run && playwright test",
    "test:unit": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "serve": "node tests/static-server.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
